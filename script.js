function processEquation() {
    // Get user input
    const equation = document.getElementById('equationInput').ariaValueMax;

    // Dummy output for now
    document.getElementById('outputArea').innerText = "You entered: " + equation;

    // Here is where parsing and uncertainty calculations will actually happen
}